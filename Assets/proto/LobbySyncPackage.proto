syntax = "proto3";

package LobbySyncPackage;

/* --------------------------------------------------
  顶层封装-大厅操作事件
-------------------------------------------------- */
message LobbySync {
  uint32 eventID = 1; // 事件ID
}